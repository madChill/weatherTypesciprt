import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles({
    table: {
        minWidth: 100,
    },
    root: {
        width: 500
    },
    avartarContent: {
        display: 'flex'
    }
});
export default useStyles;